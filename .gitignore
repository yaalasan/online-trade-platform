.venv/
__pycache__/
*.pyc

data.db

.env
.env.*
# …but keep example/template env files (no secrets) tracked.
!.env.example
!**/.env.example

.agents/
.codex/

# Binary design docs (kept locally, not in the repo)
*.odt

# local DB backups
data.db.bak-*

# migration setup carries generated DB passwords — keep only the template
migration/01_setup.sql

# Local database backups (never commit)
backups/
